using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleApp_Enums
{
    //Best practice is to define enums in their own document.
    //Declare a Enum Type Syntax:
    //accessModifier enum enumName { enumerator1, enumerator2, etc }
    //Note: items in an enum are called enumerators or elements. 
    public enum DaysOfWeek
    {
        Monday, Tuesday,Wednesday,Thursday, Friday, Saturday, Sunday
    }
    
}
